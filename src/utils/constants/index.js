export const { VITE_BASE_URL: baseURL } = import.meta.env;
export const { VITE_BASE_LINK: baseLink } = import.meta.env;
export const multipartFormData = {
  headers: {
    "Content-Type": "multipart/form-data",
  },
};
